## 📊 练习批改与分析
---

### Exercise 11.34 下标操作与find的区别

**原题：** What would happen if we used the subscript operator instead of find in the transform function?

**你的答案：**
```
如果改成了使用下标那么结果会有非常大的改变：每一次使用下标如果没有规则的话，map也会创建一个空规则。进而导致一些不需要规则去替换的字符串，直接被替换为空字符。
```

**批改：** ⭐⭐⭐⭐ 4/5 优秀答案！核心概念正确

**详细分析：**
- ✅ **核心理解正确**：准确指出了下标操作会创建新元素
- ✅ **影响分析正确**：理解到未匹配的单词会被替换为空字符串
- ⚠️ **可以更全面**：还需要提到const引用的问题和函数签名的改变

**正确答案：**

如果将`transform`函数中的`find`改为下标操作，会产生以下问题：

1. **函数签名必须改变**：下标操作会修改map，所以不能接受const引用
2. **意外插入元素**：对于不在映射中的单词，会插入一个键为该单词、值为空字符串的条目
3. **返回值错误**：未匹配的单词本应返回原单词，但使用下标操作会返回空字符串
4. **map被意外修改**：每次处理新单词都会改变trans_map，可能导致性能问题和意外行为


---

### Exercise 11.35 insert与下标操作的区别

**原题：** In buildMap, what effect, if any, would there be from rewriting `trans_map[key] = value.substr(1);` as `trans_map.insert({key, value.substr(1)})?`

**你的答案：**
```
使用下标操作的话，如果一个键或者说一个规则出现多次，那么这条规则键会被更新为最后依次出现的那个
然而于使用insert相反的是，当一个规则出现多次之后，insert只会在第一次规则出现时进行添加，后续出现重复的规则键都直接被忽略
```

**批改：** ⭐⭐⭐⭐⭐ 5/5 完美答案！

**详细分析：**
- ✅ **完全正确**：准确描述了两种操作在处理重复键时的不同行为
- ✅ **对比清晰**：明确指出了下标操作会更新，insert会保留首次插入的值

**正确答案：**

1. **下标操作**：如果键已存在，会用新值替换旧值（更新为最后一次出现的规则）
2. **insert操作**：如果键已存在，不会改变现有值（保留第一次出现的规则）


---

### Exercise 11.36 无效规则的处理

**原题：** Our program does no checking on the validity of either input file. In particular, it assumes that the rules in the transformation file are all sensible. What would happen if a line in that file has a key, one space, and then the end of the line? Predict the behavior and then check it against your version of the program.

**你的答案：**
```
会直接runtime_error"no rule for ..."
```

**批改：** ⭐⭐⭐⭐⭐ 5/5 完美答案！

**详细分析：**
- ✅ **完全正确**：程序会抛出runtime_error异常
- ✅ **异常消息准确**：指出了具体的错误信息

**正确答案：**

当规则文件中的某一行只有键、一个空格、然后行尾时：
1. `map_file >> key` 读取键
2. `getline(map_file, value)` 读取剩余部分，包含一个空格
3. `value.size()` 为1（因为空格是一个字符）
4. 不满足 `value.size() > 1` 的条件
5. 抛出 `runtime_error("no rule for " + key)`

**代码验证：**
```cpp
// 规则文件内容：
// brb be right back
// lol 
// (注意lol后面有一个空格)

// 执行过程：
// 第一行：key="brb", value=" be right back", value.size()>1 ✓
// 第二行：key="lol", value=" ", value.size()==1 ✗ → 抛出异常
```
